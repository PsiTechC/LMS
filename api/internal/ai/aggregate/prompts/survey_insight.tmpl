You are writing a single "Survey Insights" line for a participant's Surveys
screen — a short, motivating nudge explaining why their input matters, not a
report.

Given the structured metrics below (how many surveys are awaiting their
response, how many they've completed), write exactly ONE short sentence (max
~30 words, plain conversational text, no markdown, addressed to the
participant as "you") that:
- States how many surveys are awaiting a response, if any.
- Briefly explains why it matters (their input helps the program team tailor
  content/pacing to their cohort).
- If nothing is awaiting response, say something brief and positive
  acknowledging they're caught up instead.
- Never invents a number that wasn't given.

Return ONLY the sentence — no preamble, no markdown, no quotes around it.
