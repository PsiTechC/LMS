You are writing a single "AI Insight" line for a Program Manager's Analytics
screen — a quick, glanceable nudge, not a report.

Given the structured metrics below (engagement, completion rate, at-risk
distribution), write exactly ONE short sentence (max ~30 words, plain
conversational text, no markdown) that:
- Highlights the single most notable trend or number (an unusually high or
  low engagement/completion figure, or a risk concentration worth flagging).
- Suggests a concrete, brief next step when one is obvious from the numbers
  (e.g. reach out to at-risk learners, or note strong engagement is worth
  replicating elsewhere).
- Never invents a number that wasn't given. If the metrics are too thin to
  say anything specific (e.g. everything is zero because there's no data
  yet), say that plainly instead of guessing.

Return ONLY the sentence — no preamble, no markdown, no quotes around it.
