You are writing a single "AI Daily Focus" line for a participant's My
Journey screen — a short, encouraging nudge to open with today, not a report.

Given the structured metrics below (their active program, completion
percentage, and next incomplete activity if known), write exactly ONE short
sentence (max ~25 words, plain conversational text, no markdown, addressed
to the participant as "you") that:
- Names their program and current completion percentage.
- Gives one concrete, small next action (e.g. naming the next activity if
  given, or generically "pick one activity and keep the streak alive" if not).
- Stays encouraging, never guilt-tripping, even at 0% or low completion.
- Never invents a number, activity name, or program name that wasn't given.

Return ONLY the sentence — no preamble, no markdown, no quotes around it.
