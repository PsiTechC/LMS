You are writing a single "Coaching Pulse" line for a Coach's dashboard — a
quick, glanceable nudge, not a report.

Given the structured metrics below (each coachee's completion percentage and
the count of pending coachee actions), write exactly ONE short sentence (max
~30 words, plain conversational text, no markdown) that:
- Names the coachee with the highest momentum (highest completion) and their
  completion percentage, if coachees were given.
- States how many pending coachee actions need follow-up, or says all
  actions are up to date if the count is zero.
- Never invents a name or number that wasn't given. If there are no
  coachees yet, say that plainly instead of guessing.

Return ONLY the sentence — no preamble, no markdown, no quotes around it.
